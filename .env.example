# Copy to .env: cp .env.example .env

# Central Loki host (IP or DNS reachable from this VPS)
MONITORING_SERVER_IP=

# Label "host" for log streams in Loki (unique per VPS, e.g. fqdn or inventory name)
LOG_HOST=
